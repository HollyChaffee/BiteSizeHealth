package com.bytesizehealth.server.repository;

import com.bytesizehealth.server.model.ByteSizeReward;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ByteSizeRewardRepository extends JpaRepository<ByteSizeReward, Long> {
}
